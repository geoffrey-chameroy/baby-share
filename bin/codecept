#!/usr/bin/env php
<?php

if (!file_exists(dirname(__DIR__).'/vendor/codeception/codeception/codecept')) {
    echo "Unable to find the `codecept` script in `vendor/codeception/codeception/`.\n";
    exit(1);
}

require dirname(__DIR__).'/vendor/codeception/codeception/codecept';
